# Aegis Range Strategy V1 - Scientific Authority

This directory is the canonical scientific home for Aegis Range Strategy V1
after the ownership amendment dated 2026-08-24.

Authority order:

1. `AEGIS_RANGE_STRATEGY_V1_PLAN.md` and the approved historical R0 artifacts
   preserve the original scientific contract byte-for-byte.
2. `AEGIS_RANGE_R0_OWNERSHIP_AMENDMENT.md` supersedes only repository ownership
   and future phase location statements.
3. `r0_ownership_manifest.json` records the migration paths, commits, hashes and
   phase-state assertions.

Scientific R1/R2 work, if separately authorized, belongs under the parent
`trading_system` repository. The reserved research-only location is
`sandbox/aegis_range_strategy_v1/`; it does not exist yet and this migration does
not authorize creating it.

`binance-futures-bot-ts` is reserved for a later runtime port/integration after a
scientific strategy is frozen and approved. It has no authority to change the
scientific specification.

No R1, R2, TRAIN, CALIBRATION, VALIDATION or HOLDOUT action is authorized by
this directory migration.
